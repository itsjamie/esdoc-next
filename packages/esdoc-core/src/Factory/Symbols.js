const already = Symbol('already');
const external = Symbol('external');
const astKey = Symbol('ast');
const name = Symbol('name');

exports.already = already;
exports.external = external;
exports.astKey = astKey;
exports.name = name;
